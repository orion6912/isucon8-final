version: 2
jobs:
  build:
    branches:
        only:
          - master
    steps:
  deploy:
    machine:
      enabled: true
    steps:
      - run:
          name: Deploy Over SSH
          command: |
            ssh $SSH_USER@$SSH_HOST "/home/ec2-user/isucon8-final/shell/deploy.sh"